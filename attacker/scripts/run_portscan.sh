#!/bin/bash
# Phase 5 placeholder. Real usage (Milestone 4) will run something like:
#   nmap -sS -p- target
# against the target container's hostname on lab_net.
# Left deliberately minimal until the capture/detection pipeline exists
# to observe and validate against.
set -euo pipefail
echo "[run_portscan] placeholder — see docs/DETECTION_RULES.md once port_scan_v1 exists"
echo "[run_portscan] example manual command: nmap -sS -p- target"
